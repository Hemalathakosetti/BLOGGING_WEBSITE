<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %></title>
</head>
<body>
    <h1><%= post.title %></h1>
    <% if (post.image && post.image.length > 0) { %>
        <% post.image.forEach(image => { %>
            <img src="/uploads/<%= image %>" alt="<%= post.title %>">
        <% }); %>
    <% } %>
    <p><%= post.content %></p><br><br><br>
  <button color="black"> <big> <a href="/">Back to Home</a></big></button>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
  <button color="black"> <big> <a href="/editor">Edit</a><br></big></button>
</body>
</html>